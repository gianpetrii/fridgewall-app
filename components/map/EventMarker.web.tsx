// Web stub — react-native-maps is not supported on web
import { AppEvent } from '../../types';

interface EventMarkerProps {
  event: AppEvent;
  onPress: (event: AppEvent) => void;
}

export function EventMarker(_props: EventMarkerProps) {
  return null;
}
